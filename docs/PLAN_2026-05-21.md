# Plan — 2026-05-21

Continues the 7-phase refactor (see `docs/REFACTOR_STATUS.md`).

**2026-05-20 EOD status:** all 7 phases shipped; `main` (`8c60955` → `825a790`)
**pushed + Vercel-deployed**; migrations `035`–`038` applied to prod;
`process-jobs` pg_cron **wired + verified** (`CRON_SECRET` in Vercel env,
`curl` → `{"ok":true,...}`). P0 steps 1 + 2 DONE.

## P0 — ship what's built

1. ~~**Push `main`** → Vercel auto-deploys.~~ ✅ DONE 2026-05-20.
2. ~~**Wire the job-worker pg_cron.**~~ ✅ DONE 2026-05-20 — `pg_cron`+`pg_net`
   enabled, `cron.schedule('process-jobs', '* * * * *', ...)` registered
   (jobid 1), verified live.
   - ⏳ STILL TODO: confirm the `023` sync-platforms cron is also scheduled —
     `SELECT jobid, jobname, schedule, active FROM cron.job;` (expect 2 rows).
3. **Smoke test prod:** login, cart checkout (paid path + free-checkout path),
   realtime messaging (two browsers), `POST /api/internal/jobs/process`
   returns `{ ok: true, ... }`, a registration enqueues + sends the welcome
   email.

## P0 — close verification gaps

4. **Checkout + webhook smoke test** — the payments module is a
   behavior-preserving extraction with no automated test. Verify: paid
   checkout, free-checkout (Razorpay unconfigured), webhook idempotency
   (replay a captured event → no double finalize).
5. **Provision a Supabase test project** → set `SUPABASE_TEST_URL` +
   `SUPABASE_TEST_SERVICE_ROLE_KEY`, apply migrations, run
   `cd server && npm test` → the 30 auth integration tests go green.

## P1 — decision-gated (decide, then build)

6. **Semantic creator search** — pick an embedding model (e.g. OpenAI
   `text-embedding-3-small`, 1536-dim) + API key + cost sign-off. Then a
   migration adds `influencer_profiles.embedding vector(N)` + an HNSW index;
   an `embedding` job backfills + keeps it fresh. `vector` extension already
   enabled (migration `038`).
7. **Escrow + automated payouts** — choose a payout provider (RazorpayX);
   needs product + legal sign-off on the escrow flow. Out of scope for a
   code-only pass until decided.
8. **Fraud detection heuristics** — define the signal set (follower spikes,
   low authenticity score, checkout velocity), then a pure scoring service
   in the shape of `campaignFitService` / `authenticityScoreService`.

## P2 — optional, no decision needed

9. Fold `workflowController` + `workflowActions` into `modules/campaigns/`
   (finishes the encapsulation cleanup).
10. More job handlers as real async work appears — analytics sync, upload
    post-processing, webhook retries. (Notifications/invoices are NOT
    candidates yet — see `REFACTOR_STATUS.md`.)
11. Live brand/creator dashboard counters over realtime.

## Notes / guardrails

- Standardized API responses (`utils/apiResponse.ts`): **recommend skipping**
  the retrofit — current response shapes vary and changing them breaks the
  client. Use a convention for new endpoints only.
- Keep latency-sensitive emails (OTP, password reset) synchronous — the job
  worker runs once a minute.
- Migrations: always a new numbered file; never edit `035`–`038` (applied).
